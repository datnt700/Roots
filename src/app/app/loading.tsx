// app/app/loading.tsx — Loading UI for /app/* routes
// Inlined (not re-exported from parent dir) to avoid Turbopack cross-boundary chunk confusion.
import Loading from '@/app/loading'
export default Loading
